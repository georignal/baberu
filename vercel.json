{
  "buildCommand": "npm install --prefix ../server && npm install $(node -e \"console.log(Object.keys(require('../server/package.json').dependencies).join(' '))\") && npm install && npm run build",
  "outputDirectory": "dist"
}
