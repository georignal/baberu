{
  "buildCommand": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && cp -r client/dist/* .",
  "functions": {
    "api/*.ts": {
      "runtime": "@vercel/node@4"
    }
  }
}
